from setuptools import setup, find_packages

setup(
    name='wf3-local-processing',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Các thư viện cần thiết
    ],
    author="Wealth Farming",
    description="Wealth Farming - Local Processing",
)
